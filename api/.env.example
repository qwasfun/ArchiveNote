# Copy this file to .env and fill in secure values. Do NOT commit real secrets to source control.
# Generate a secure SECRET_KEY, e.g. in Python: `python -c "import secrets; print(secrets.token_urlsafe(32))"`

SECRET_KEY=change-me-generate-a-secure-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_ALGORITHM=HS256
# DATABASE_URL 非必填，如果未配置 DATABASE_URL，默认使用 sqlite
DATABASE_URL=postgres://postgres:password@localhost:5432/postgres

# Storage Configuration
# 存储类型: local 或 s3
STORAGE_TYPE=local

# S3 存储配置（仅当 STORAGE_TYPE=s3 时需要）
# 支持 AWS S3, MinIO, 阿里云 OSS, 腾讯云 COS 等 S3 兼容存储
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_BUCKET_NAME=your-bucket-name
# S3 端点 URL（AWS S3 可以不填，MinIO 等需要填写，如 http://localhost:9000）
S3_ENDPOINT_URL=https://s3.ap-southeast-1.amazonaws.com
# S3 区域（AWS S3 需要，如 us-east-1，其他服务可选）
S3_REGION_NAME=ap-southeast-1
# 公共访问 URL（可选，用于直接访问文件）
S3_PUBLIC_URL=
